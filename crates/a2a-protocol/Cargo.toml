[package]
name = "a2a-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared A2A task types for Sentinel<->Forge delegated verification (issue #143)"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
